<!DOCTYPE html>
<html>

<head>
    <title>Edit Post</title>
</head>

<body>
    <h1>Edit Post</h1>
    <form method="post" action="/posts/<%= post.id %>/edit">
        <label>Title: <input type="text" name="title" value="<%= post.title %>" required></label><br>
        <label>Content:<br>
            <textarea name="content" rows="8" cols="40">
                <%= post.content %>
            </textarea>
        </label><br>
        <label>Publish: <input type="checkbox" name="published" <%=post.published ? "checked" : "" %>></label><br>
        <button type="submit">Update</button>
    </form>
    <a href="/posts">Back to Posts</a>
</body>

</html>